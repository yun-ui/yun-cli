#!/usr/bin/env node

const program = require('commander')

program
    .version(require('../package').version)
    .usage('[Options] <project_name>')
    .command('init <project_name>', 'init a vue.js project with yun-ui template.')
    .parse(process.argv)
